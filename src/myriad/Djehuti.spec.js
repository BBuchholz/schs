/**
 * @file Djehuti is the main controller for the Basecamp operations
 * of Myriad.
 */

const Djehuti = require('./Djehuti');

const djehuti = Djehuti();

test('should have at least one test', () => {

  expect(true).toBe(true);

});